/* Puente al nucleo. Solo existe en Energy: es la ruta que consulta el asistente
   de WhatsApp para calcular sin hacer la cuenta el mismo. */
export { POST } from "@voltac/core/dimensionamiento/api/route";

export const dynamic = "force-dynamic";
